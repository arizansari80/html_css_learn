CREATE TABLE ISSUE
(
ISBN BIGINT NOT NULL ,
ROLL VARCHAR(12) NOT NULL,
ISSUE_DATE DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
EXP_RETURN_DATE DATETIME NOT NULL 
);

INSERT INTO ISSUE(ISBN,ROLL,EXP_RETURN_DATE) VALUES
(1254745631254,'14BCS0058',NOW() + INTERVAL 15 DAY);
